[project]
name = "cupscale-python-tests"
version = "0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "opencv-python-headless",
    "torch",
    "pytest",
]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.pytest.ini_options]
testpaths = ["."]
